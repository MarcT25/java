# Programs I done in Java

1) Palindrome: This project checks if a word is a palindrome using a stack.
2) WordCounter: WordCounter counts the word, spaces, sentences and other things in a given text file.
